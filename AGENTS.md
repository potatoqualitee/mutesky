# Repository Notes

- The authoritative source for permanent keyword categories is the
  `potatoqualitee/calm-the-chaos` repository, which powers the corresponding
  Calm the Chaos Chrome extension. Make permanent keyword changes there first;
  MuteSky consumes that catalog and should only mirror metadata needed for its
  fetch fallback or client behavior.
